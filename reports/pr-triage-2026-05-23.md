# PR Triage Report - 2026-05-23

Repository: `ysdede/parakeet.js`
Scope: Open PR backlog (`#128` to `#190`), 61 open PRs total.
Primary goal: reduce duplicate bot PR noise and keep only low-risk, non-breaking improvements.

## Executive Summary

Recommended merge-now bundle (low-risk, validated together):
- `#132` Fix ORT-web graph capture bug
- `#133` Concurrent download of optional files
- `#148` FrameAlignedMerger overlap-loop optimization
- `#179` Argmax loop direct-access optimization
- `#183` Softmax `Math.exp` accumulation optimization
- `#186` LCS loop-invariant code motion
- `#187` O(N) sentence boundary mapping
- `#188` Tensor tracking allocation optimization in decoder hot path
- `#189` 8x loop unroll in `normalizeFeatures`

Combined branch prepared locally: `dev/1.4.5`
Package version bumped on the branch: `1.4.4` -> `1.4.5`

Validation on combined branch:
- `npx vitest run --reporter=dot --silent` -> **13/13 test files, 131/131 tests passed**
- `node tests/test_mel.mjs` -> **all mel validation/perf checks passed**

## Classification By Cluster

### 1) Comment-only / low-value churn
Action: Close
PRs:
- `#128`, `#129`, `#130`, `#131`
Reason:
- Non-functional comment cleanup; high review noise, minimal product value.

### 2) ORT graph-capture correctness
Action: Merge
PRs:
- `#132`
Reason:
- Fixes option-shape behavior (`enableGraphCapture`) and retry path robustness.

### 3) Optional file download concurrency
Action: Merge
PRs:
- `#133`
Reason:
- Parallelizes optional `.data` fetches, low logic risk.

### 4) Tensor disposal/object iteration hot-path variants
Action: Keep one, close rest
Pick:
- Merge `#188`
Close as duplicates/older variants:
- `#134`, `#141`, `#145`, `#151`, `#158`, `#165`, `#167`, `#171`, `#172`, `#174`, `#184`, `#185`
Reason:
- Same optimization family (`Set`/`Object.values` -> array/`for...in`), many duplicates.
- `#188` is the cleanest recent representative.
Notes:
- `#172` also touches encoder output fallback retrieval; optional to re-apply later if desired.
- `#174` adds `profile.cpuprofile` artifact; not ideal for merge.

### 5) Argmax-loop variants
Action: Keep one, close rest
Pick:
- Merge `#179`
Close:
- `#135`, `#138`, `#139`, `#140`, `#144`, `#147`, `#154`, `#155`, `#175`
Reason:
- Same direct-access unrolled argmax idea, mostly cosmetic variations.

### 6) LCS loop-invariant hoist (LICM)
Action: Keep one, close rest
Pick:
- Merge `#186`
Close:
- `#156`, `#157`, `#162`, `#173`, `#190`
Reason:
- Same `const xi = X[i - 1]` hoist optimization; `#186` is minimal and clean.

### 7) LCS heavy unrolling variants
Action: Close for now (or park)
PRs:
- `#142`, `#143`, `#146`, `#150`, `#152`, `#153`
Reason:
- Large complexity increase for micro-optimization; higher regression risk than LICM-only approach.

### 8) Sentence boundary mapping (O(N+M))
Action: Keep one, close rest
Pick:
- Merge `#187`
Close:
- `#176`
Reason:
- Equivalent two-pointer optimization family; `#187` is more explicit on constraints.

### 9) Mel normalization single-pass variance
Action: Reject/close
PRs:
- `#136`, `#163`, `#166`
Reason:
- Single-pass variance formulation has numerical-stability risk vs current stable two-pass behavior.

### 10) Mel normalization 8x unroll (stable two-pass)
Action: Keep one, close rest
Pick:
- Merge `#189`
Close:
- `#168`, `#169`, `#170`, `#177`
Reason:
- Same optimization family with stable math preserved.

### 11) FFT inner-loop/interchange variants
Action: Defer as optional follow-up
PRs:
- `#159`, `#160`, `#164`
Recommendation:
- Keep only one candidate (`#160`) if future profiling shows measurable end-to-end win.
Reason:
- Potential gain is plausible but should be benchmark-gated to avoid maintenance churn.

### 12) FrameAlignedMerger duplicate-check optimization
Action: Keep one, close rest
Pick:
- Merge `#148`
Close:
- `#137`
Reason:
- Same reverse-scan early-exit idea; `#148` is the clearer representative.

### 13) Misc unique PRs
Action:
- Close `#149` (bench scripts only; move to separate perf-tooling branch if needed)
- Defer/close `#161` (tiny `BigInt64Array` micro-opt)
- Optional later `#178` (frame confidence aggregation cleanup)
- Defer `#181` (32x transpose unroll; maintainability cost, benchmark-gate first)
- Merge `#183` (selected above)

## Proposed Next Decision Set

If you want fastest cleanup with low break risk:
1. Merge the 9-PR bundle (`#132`, `#133`, `#148`, `#179`, `#183`, `#186`, `#187`, `#188`, `#189`).
2. Close duplicate PRs in each cluster.
3. Park or close high-churn/high-complexity micro-optimization branches (`LCS unroll`, `transpose 32x`, `single-pass variance`).

## Local Evidence Snapshot

Commands run during triage:
- `npm test` (baseline on `master`): pass
- `npx vitest run --reporter=dot --silent` (multiple representative PR branches): pass
- `npx vitest run --reporter=dot --silent` (combined branch): pass (13 files, 131 tests)
- `node tests/test_mel.mjs` (combined branch): pass
