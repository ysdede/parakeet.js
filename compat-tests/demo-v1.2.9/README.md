# Backward Compatibility Demo (v1.2.9)

This folder contains an archived demo app from the `v1.2.9` era.

It is used only to verify backward compatibility of newer `parakeet.js` releases
with projects originally built against `1.2.x`.

Quick check:

```bash
npm install
npm run build
```
